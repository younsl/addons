{{- range $policyKey, $policyValue := .Values.mutatingAdmissionPolicies }}
{{- if $policyValue.enabled }}
{{- $policyName := include "kubernetes-admission-policies.policyName" (dict "key" $policyKey "value" $policyValue) }}
{{- range $bindingKey, $bindingValue := $policyValue.bindings }}
{{- if $bindingValue.enabled }}
---
apiVersion: {{ $.Values.apiVersions.mutatingAdmissionPolicy }}
kind: MutatingAdmissionPolicyBinding
metadata:
  name: {{ include "kubernetes-admission-policies.bindingName" (dict "policyName" $policyName "key" $bindingKey "value" $bindingValue) }}
  labels:
    {{- include "kubernetes-admission-policies.labels" $ | nindent 4 }}
  {{- with $.Values.commonAnnotations }}
  annotations:
    {{- tpl (toYaml .) $ | nindent 4 }}
  {{- end }}
spec:
  policyName: {{ $policyName }}
  {{- with $bindingValue.matchResources }}
  matchResources:
    {{- tpl (toYaml .) $ | nindent 4 }}
  {{- end }}
  {{- with $bindingValue.paramRef }}
  paramRef:
    {{- tpl (toYaml .) $ | nindent 4 }}
  {{- end }}
{{- end }}
{{- end }}
{{- end }}
{{- end }}
