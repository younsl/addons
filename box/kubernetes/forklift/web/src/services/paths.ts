// Every route in the OpenAPI document is mounted under this prefix. The
// generated services interpolate it rather than hardcoding it, so a change here
// moves the whole client.
export const API_PREFIX = "/api/v1";
