# Build artifacts
/bin/
/forklift
# Staged into the image build context by `make docker-context`
/forklift-linux-*
/forklift-mcp-linux-*
/target/
cover.out
*.test
*.profraw

# Local data dir
/.data*/

# Throwaway data dir for browser tests, plus the signed-in cookies
# global setup leaves for them
/.e2e-data/
web/test/e2e/.auth/

# Local config files
/config/
!config

# Node / Vite
web/node_modules/
web/.vite/
web/playwright-report/
web/storybook-static/
web/test-results/
site/node_modules/
site/.vite/
site/dist/
/.pnpm-store/

# Embedded UI build output from `make web-build`. None of it is committed: a
# binary built without it serves src/webui/placeholder.html instead.
src/webui/dist/

# Packaged Helm charts
*.tgz
