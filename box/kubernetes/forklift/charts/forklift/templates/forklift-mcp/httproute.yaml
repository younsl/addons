{{- if and .Values.mcp.enabled .Values.mcp.gateway.enabled -}}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ .Values.mcp.gateway.name | default (printf "%s-mcp" (include "forklift.fullname" .)) }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "forklift.mcp.labels" . | nindent 4 }}
spec:
  parentRefs:
    {{- range .Values.mcp.gateway.parentRefs }}
    - name: {{ .name }}
      {{- if .group }}
      group: {{ .group }}
      {{- end }}
      {{- if .kind }}
      kind: {{ .kind }}
      {{- end }}
      {{- if .namespace }}
      namespace: {{ .namespace }}
      {{- end }}
      {{- if .sectionName }}
      sectionName: {{ .sectionName }}
      {{- end }}
    {{- end }}
  {{- if .Values.mcp.gateway.hostnames }}
  hostnames:
    {{- range .Values.mcp.gateway.hostnames }}
    - {{ . | quote }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.mcp.gateway.rules }}
    - matches:
        {{- range .matches }}
        - path:
            type: {{ .path.type }}
            value: {{ .path.value }}
        {{- end }}
      {{- with .filters }}
      filters:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      backendRefs:
        {{- range .backendRefs }}
        - group: {{ .group | quote }}
          kind: {{ .kind | quote }}
          name: {{ .name | default (printf "%s-mcp" (include "forklift.fullname" $)) }}
          port: {{ .port | default $.Values.mcp.service.port }}
          weight: {{ .weight }}
        {{- end }}
    {{- end }}
{{- end }}
