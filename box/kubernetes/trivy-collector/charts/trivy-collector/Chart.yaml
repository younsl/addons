apiVersion: v2
name: trivy-collector
description: Multi-cluster Trivy report collector and viewer
type: application
deprecated: false
version: 0.12.1
appVersion: "1.7.1"
keywords:
  - trivy
  - security
  - vulnerability
  - sbom
  - kubernetes
home: https://github.com/younsl/o
sources:
  - https://github.com/younsl/o/tree/main/box/kubernetes/trivy-collector
maintainers:
  - name: younsl
    email: cysl@kakao.com
    url: https://github.com/younsl
annotations:
  artifacthub.io/changes: |
    - kind: changed
      description: "Replace the horizontal header with a foldable sidebar; menus regrouped and search/admin pages promoted to top-level entries"
    - kind: changed
      description: "Replace Swagger UI with an embedded Scalar reference at /api-docs; the bundle ships in the image, and a CSP plus config opt-out keep it from reaching Scalar's hosted platform"
    - kind: removed
      description: "Remove /swagger-ui; the OpenAPI document stays at /api-docs/openapi.json"
    - kind: fixed
      description: "A scraper configured to watch nothing no longer reports a permanent rebuild; hydration now separates 'nothing to watch' from 'still syncing'"
    - kind: fixed
      description: "export-state Job inherits the scraper's nodeSelector, tolerations and affinity; without them it stays Pending on a tainted node and blocks the pre-sync hook it precedes"
